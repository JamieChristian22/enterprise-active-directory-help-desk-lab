# Capacity Management Plan
Tracks CPU, memory, disk latency, datastore free space, backup growth, directory database size, DHCP utilization, certificate issuance, and patch window duration. Forecasting uses 30/60/90-day trends and triggers action before sustained utilization exceeds defined thresholds.
