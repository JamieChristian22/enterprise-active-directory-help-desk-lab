# Hybrid Identity Operations
Covers Microsoft Entra Connect staging mode, sync health, source anchor consistency, UPN alignment, password hash synchronization, soft-match conflicts, duplicate proxy addresses, and controlled failover. The runbook requires export validation, connector-space review, change approval, and post-sync verification before closure.
