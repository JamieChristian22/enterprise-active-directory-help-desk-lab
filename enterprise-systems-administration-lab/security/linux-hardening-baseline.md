# Linux Hardening Baseline

Includes SSH key authentication, disabled root login, sudo controls, host firewall, automatic security updates, auditd, file permissions, time synchronization, log forwarding, and service minimization.
