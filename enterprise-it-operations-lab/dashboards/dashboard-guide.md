# Dashboard Guide

Create four KPI cards: availability, patch compliance, backup success, and SLA attainment. Add ticket volume by category, average resolution by priority, monthly trend lines, and a table of failed backup or overdue patch exceptions. Use `monthly-kpis.csv` and `tickets/resolved-tickets.csv`.
