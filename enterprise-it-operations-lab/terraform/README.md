# Terraform Lab

This configuration creates a minimal AWS operations foundation: VPC, public/private subnet examples, CloudWatch log group, and SNS alert topic. Run `terraform fmt`, `terraform validate`, `terraform plan`, and review cost/security impact before apply. Supply `alert_email` through a `.tfvars` file that is not committed.
