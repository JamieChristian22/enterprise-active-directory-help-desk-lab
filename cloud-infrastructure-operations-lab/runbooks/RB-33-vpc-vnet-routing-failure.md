# VPC/VNet Routing Failure
Capture source, destination, protocol, and expected path. Validate route tables, effective routes, security groups/NSGs, NACLs, firewall rules, peering transit settings, DNS resolution, and VPN/BGP status. Use flow logs and packet tests to identify the deny point, implement the least-privilege correction, and verify both directions.
